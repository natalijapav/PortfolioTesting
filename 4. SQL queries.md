# MySQL Queries with Example Results

This document contains example MySQL queries executed on `characters` and `library` tables, with example results to demonstrate output formatting.

---

## 1. Select all characters
```sql
SELECT *
FROM characters;
